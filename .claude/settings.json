{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -f .build/ci/bootstrap-repo.sh ] || { [ -e .build/.git ] && git -C .build symbolic-ref -q HEAD >/dev/null 2>&1; } || git submodule update --init --recursive -q 2>/dev/null; [ -f .build/ci/bootstrap-repo.sh ] && bash .build/ci/bootstrap-repo.sh || true"
          },
          {
            "type": "command",
            "command": "git config core.hooksPath .githooks"
          },
          {
            "type": "command",
            "command": "echo 'riviere workspace ready - git hooks configured'"
          },
          {
            "type": "command",
            "command": "bash -c 'command -v gh &>/dev/null && gh auth status &>/dev/null 2>&1 && echo \"CI_MONITORING=gh\" || echo \"CI_MONITORING=fallback\"'"
          }
        ]
      }
    ]
  }
}
